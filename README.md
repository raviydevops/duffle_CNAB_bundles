# aws cdk for spring boot application that uses dynamodb

AWS CDK is used to deploy the springboot dynamodb app
``` no-highlight
cd ts_cdk
npm run build
cdk ls
cdk deploy fargate
cdk destroy fargate
```