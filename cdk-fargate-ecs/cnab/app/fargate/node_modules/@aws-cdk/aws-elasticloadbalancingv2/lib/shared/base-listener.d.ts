import cdk = require('@aws-cdk/cdk');
import { ITargetGroup } from './base-target-group';
/**
 * Base class for listeners
 */
export declare abstract class BaseListener extends cdk.Construct implements cdk.IDependable {
    readonly dependencyElements: cdk.IDependable[];
    readonly listenerArn: string;
    private readonly defaultActions;
    constructor(parent: cdk.Construct, id: string, additionalProps: any);
    /**
     * Validate this listener
     */
    validate(): string[];
    /**
     * Add a TargetGroup to the list of default actions of this listener
     */
    protected _addDefaultTargetGroup(targetGroup: ITargetGroup): void;
}
