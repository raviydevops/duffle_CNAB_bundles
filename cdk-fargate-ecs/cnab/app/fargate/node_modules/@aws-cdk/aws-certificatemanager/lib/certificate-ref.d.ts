import { Construct } from "@aws-cdk/cdk";
/**
 * Interface for certificate-like objects
 */
export declare abstract class CertificateRef extends Construct {
    /**
     * Import a certificate
     */
    static import(parent: Construct, name: string, props: CertificateRefProps): CertificateRef;
    abstract readonly certificateArn: string;
    /**
     * Export this certificate from the stack
     */
    export(): CertificateRefProps;
}
/**
 * Reference to an existing Certificate
 */
export interface CertificateRefProps {
    /**
     * The certificate's ARN
     */
    certificateArn: string;
}
