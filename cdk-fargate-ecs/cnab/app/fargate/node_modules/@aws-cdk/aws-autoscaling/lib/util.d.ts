import cdk = require('@aws-cdk/cdk');
/**
 * Allow lazy evaluation of a list of dependables
 */
export declare class LazyDependable implements cdk.IDependable {
    private readonly dependableSource;
    constructor(dependableSource: cdk.IDependable);
    readonly dependencyElements: cdk.IDependable[];
}
