import { Construct } from "@aws-cdk/cdk";
/**
 * Imported or created hosted zone
 */
export declare abstract class HostedZoneRef extends Construct {
    static import(parent: Construct, name: string, props: HostedZoneRefProps): HostedZoneRef;
    /**
     * ID of this hosted zone
     */
    abstract readonly hostedZoneId: string;
    /**
     * FQDN of this hosted zone
     */
    abstract readonly zoneName: string;
    /**
     * Export the hosted zone
     */
    export(): HostedZoneRefProps;
}
/**
 * Reference to a hosted zone
 */
export interface HostedZoneRefProps {
    /**
     * Identifier of the hosted zone
     */
    hostedZoneId: string;
    /**
     * Name of the hosted zone
     */
    zoneName: string;
}
/**
 * Imported hosted zone
 */
export declare class ImportedHostedZone extends HostedZoneRef {
    readonly hostedZoneId: string;
    readonly zoneName: string;
    constructor(parent: Construct, name: string, props: HostedZoneRefProps);
}
